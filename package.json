{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "echo 'No frontend build needed for API-only project'",
        "dev": "echo 'No frontend dev needed for API-only project'"
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}
